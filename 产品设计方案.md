# 校园活动与机会平台 · 产品设计方案

| 项目 | 内容 |
| --- | --- |
| 文档版本 | v1.0 |
| 对应需求 | `产品设计思路.txt` |
| 产品形态 | 面向在校学生的 Web 应用（移动端优先） |
| 设计重点 | 新生使用体验、活动"值不值得参与"的判断辅助、多来源信息治理 |
| 交付范围 | 本文档为设计方案，不含代码实现 |

---

## 1. 需求解读

### 1.1 原始要求拆解

需求原文包含四个层次的目标，它们的技术难度差异很大：

| 原文要求 | 本质问题 | 难度 |
| --- | --- | --- |
| 组织者发布活动信息，支持 PPT/Word/图片/txt/md 文件 | 富格式内容的**上传与在线浏览** | 中（格式转换是主要成本） |
| 在线浏览活动资料 | 让用户不下载就能看懂内容 | 中 |
| 帮助辨别活动是否值得参与 | **决策支持**：把原始资料转成可比较的判断依据 | 高（产品设计难点） |
| 学生自主发布活动、招募伙伴 | 供给侧开放 + 社交功能 | 中高（治理与风险） |
| 多来源信息可能产生的问题 | 真实性、重复、过期、营销噪声 | 高（治理机制） |

**结论：最基础的"发布信息"是入口，但产品的真正价值在"判断"，产品的真正风险在"多来源"。** 方案据此把功能分为三层：发布层（P0 必须做对）、判断层（差异化）、治理层（决定产品能否长期存活）。

### 1.2 三个用户角色

| 角色 | 核心诉求 | 关键痛点 |
| --- | --- | --- |
| **新生**（重点） | 这个活动是什么、我要不要一个人去、能得到什么 | 不认识地点、不知道门槛、怕白跑、信息看不懂 |
| 普通学生 | 快速找到对口的机会，认识同伴 | 信息散落在群聊/公众号/海报，过期信息干扰 |
| 组织者（校方/社团/学生个人） | 低成本把信息发出去，招到人 | 现有渠道触达差、报名统计靠问卷、资料无人看 |

### 1.3 核心矛盾与设计取舍

1. **发布门槛低 ↔ 信息质量高**：门槛高则供给少，门槛低则噪声多。→ 用**主体分级**代替一刀切审核，认证主体直发、未认证主体先审后发。
2. **附件是原始材料 ↔ 用户需要快速判断**：PPT 有 30 页，用户只想知道"值不值得去"。→ 采用**双轨制**：结构化字段负责"判断"，附件负责"深入了解"。
3. **信息多来源 ↔ 用户要可信**：同一活动可能被 5 个人重复发。→ **主体身份徽章 + 主办方"认领"机制 + 去重提示**。
4. **用户想要全部功能 ↔ 团队资源有限**：→ 严格分期，P0 只做"发布 + 浏览 + 判断卡"。

---

## 2. 产品定位与设计原则

> **一句话定位**：一个让新生在三分钟内判断"这个活动值不值得去"、并让学生自己也能发布活动与招募伙伴的校园机会平台。

**五条设计原则**

1. **决策优先**：任何页面都要先回答"值不值得"，再展示"是什么"。
2. **结构化 + 附件双轨**：能被筛选、比较、检索的信息必须是结构化字段；无法结构化的细节放附件，且必须能在线看。
3. **主体可信度永远可见**：每条信息旁必须能看出"谁发的、靠不靠谱"，不允许匿名发布。
4. **过期即归档**：活动结束后自动标记，默认从列表移除。过期信息是校园平台最大的噪声源。
5. **新生默认友好**：新用户首次进入不做空列表，而是引导 + 现成的"新生友好"内容。

---

## 3. 核心功能设计

### 3.1 发布信息（最基本功能，P0）

**多步表单**，每步不超过 5 个字段，支持草稿自动保存（新生/学生用手机填，中途易中断）：

```
步骤 1 基本信息   标题 · 活动类型 · 封面图 · 面向人群 · 一句话简介
步骤 2 时间地点   开始/结束时间 · 报名截止 · 校区 · 楼栋房间 · 线上/线下
步骤 3 参与方式   名额 · 费用 · 参与门槛 · 收获标签 · 报名方式
步骤 4 活动资料   附件上传（多文件）· 资料说明
步骤 5 预览提交   按主体类型 直发 / 送审
```

关键字段设计（决定后续"判断卡"能否成立）：

| 字段 | 类型 | 为何必须 |
| --- | --- | --- |
| 收获标签 | 多选：第二课堂学分 / 志愿工时 / 证书 / 技能 / 人脉 / 纪念品 | 新生判断"值不值得"的第一依据 |
| 参与门槛 | 枚举：零基础可参加 / 需一定基础 / 需面试筛选 | 消除"我能不能去"的疑虑 |
| 面向人群 | 不限 / 大一新生优先 / 限本院系 / 限特定专业 | 判断"是不是给我准备的" |
| 报名方式 | 站内报名 / 外部链接（问卷）/ 扫码进群 | **务实取舍**：强行站内报名会让组织者放弃使用，必须允许外链 |
| 名额与已报名数 | 数值 | 制造确定性，也提示紧迫度 |

### 3.2 附件在线浏览（P0）

需求明确要求支持 PPT、Word、图片、txt、md。**不能要求用户下载 30 页 PPT 才能判断**，因此设计转换流水线：

| 格式 | 预览方案 | 兜底策略 |
| --- | --- | --- |
| `.pptx` / `.ppt` | LibreOffice 无头转 PDF → PDF.js 逐页渲染；先生成前 5 页给首屏，其余按需转 | 转换失败则提供原件下载 + 提示 |
| `.docx` / `.doc` | 转 PDF 渲染（版式保真）；可选同时用 mammoth 转 HTML 便于检索 | 同上 |
| `.png` / `.jpg` / `.webp` / `.gif` | 直接渲染，生成多尺寸缩略图 | — |
| `.txt` | 编码探测（UTF-8 / **GBK**）后纯文本渲染 | 探测失败按 UTF-8 强解 |
| `.md` | markdown-it 渲染 + 代码高亮 + 外链图片代理 | — |

**流水线**：上传 → 原件存对象存储 → 入队 → 转换 Worker → 产物入库 → 状态回写。

状态机：`pending → processing → ready / failed`。前端在 `pending/processing` 时显示"正在生成预览"，不阻塞发布。

**两个易踩的坑，需在实现前确定**：
- 中文 txt 大量为 GBK 编码，直接按 UTF-8 读会乱码，**必须做编码探测**。
- 转换是 CPU 密集型且要跑别人的文档，**必须放独立容器、禁外网、限时限额**。

**安全约束**（P0 即需落地，不能后补）：

- 扩展名白名单 + **magic number 双重校验**（防改扩展名上传可执行文件）
- 拒绝含宏格式：`.docm` / `.pptm` / `.xlsm`
- 单文件 ≤ 50MB，单活动 ≤ 10 个附件
- ClamAV 病毒扫描，命中即隔离并告警

**进阶价值（P1）**：把 pptx/docx/pdf 的文本抽取进全文索引，这样"资料里提到招新的活动"也能被搜到，而不只是标题。

### 3.3 "一页看懂"决策卡（核心差异点，P1）

这是需求中"帮助辨别活动是否值得参与"的落地形态。详情页顶部固定一张卡，一屏内给全判断依据：

```
⚡ 一页看懂
收获   第二课堂学分 0.5 · 志愿工时 4h
时长   2 小时（周三 14:00–16:00）
门槛   零基础可参加 · 大一新生优先
名额   已报名 32/40
冲突   ⚠ 与你的《高等数学》课程时间重叠
往届   4.3/5（128 人评价）· 常见评价："能上手实操"
```

设计要点：
- **冲突检测**按成本递进：P1 仅检测"与本平台已报名/已收藏活动"的时间冲突；P2 支持导入 `.ics` 课表（比对接教务系统耦合低得多，是更现实的路径）。
- **往届口碑**必须来自**实际参与过的用户**（凭报名记录解锁评价入口），否则刷分无成本。
- 缺少数据时**不编造**：无评价就显示"暂无往届评价"，并降权排序。

### 3.4 发现与检索（P0）

- **筛选维度**：时间（今天 / 本周 / 本周末 / 自定义）、校区、活动类型、收获标签、门槛、是否新生优先、是否还可报名。
- **排序**：默认"综合"（报名截止紧迫度 + 主体信誉 + 新鲜度）；可选"最新发布""即将截止"。
- **检索**：标题 + 摘要 + 标签 + 附件抽取文本。
- **列表卡片**必须直接显示判断要素（收获标签、时长、名额、主体徽章），避免"点进去才发现不合适"。

### 3.5 参与与报名（P1）

- 报名方式三选一：站内报名（生成签到码）/ 外链跳转 / 扫码入群，**均记录"报名意图"**，用于统计与后续评价解锁。
- 收藏、日历导出（`.ics`）、开抢前提醒。
- 名额满 → 自动进入候补队列，有人取消则按序通知。

### 3.6 多来源信息治理（P1 必做）

需求明确指出"多来源信息进入平台后可能产生的问题"，这是产品能否长期存活的关卡。

**主体分级策略**

| 主体类型 | 如何认证 | 发布策略 | 展示标识 |
| --- | --- | --- | --- |
| 校方 / 院系 | 官方域名邮箱 + 后台开通 | 直发 + 事后抽检 | 官方 ✓ |
| 注册社团 / 学生组织 | 负责人认证 + 指导单位确认 | 直发 + 抽检 | 认证组织 ✓ |
| 在校学生个人 | 学号或校园邮箱认证 | 前 2 条人工审核，之后直发 + 抽检 | 个人（未认证） |
| 校外机构 | 资质材料 + 人工审核 | 逐条审核，限商业类目 | 商家 |

**四道治理机制**

1. **主体主页与信誉**：展示该主体历史活动数、平均评分、被举报次数。信誉是遏制低质发布最有效的软约束。
2. **举报与处置**：任何页面可举报（虚假信息 / 营销广告 / 内容不当 / 已结束未下架）。处置动作分四级：下架 → 限期整改 → 限制发布权限 → 封禁。承诺 24 小时内响应。
3. **自动归档**：结束时间一过自动置为 `ended`，列表默认过滤，详情页顶部灰条提示"该活动已于 X 月 X 日结束"，报名按钮变灰。**这是防误导最有效的一条规则。**
4. **重复与认领**：同一时间地点高度相似的活动发布时提示"可能重复，是否关联？"；允许主办方**认领**他人搬运的活动，认领后获得编辑权并展示官方标识（解决冷启动期运营搬运的合法性问题）。

### 3.7 招募伙伴 / 找搭子（P2）

需求提到"学生自主发布活动、招募伙伴时的实际需求"。这是个高风险社交功能，采用**限定式设计**：

- **只允许基于活动组队，不开放独立社交广场**（大幅降低陌生人风险与审核成本）。
- 队长发起组队：设人数上限、可见范围（全站 / 仅同院系）、加入是否需要批准。
- 成员展示：昵称 + 院系 + 年级，**不展示学号、手机号、微信**。
- 联系仅站内私信；提供屏蔽与举报。
- 明文规则：禁止发布非活动相关的交友、交易、代抢信息。

### 3.8 新生引导（P0 结尾 / P1 初，重点需求）

- **首次进入三步引导**：选院系与年级 → 选 3 个兴趣标签 → 生成个性化发现流（避免空列表）。
- **新生专区**：聚合"零门槛 + 无需组队 + 有引导人 + 时间短"的活动，标签 `新生友好`。
- **地点要说人话**：不只写"三教 305"，同时给"从 XX 宿舍步行 8 分钟"+ 地图跳转。
- **校园节奏提示**：开学季 / 期中 / 期末各阶段常见机会类型的轻量提示，弥补新生"不知道有什么可参加"的信息差。

### 3.9 管理后台（P1）

待审队列（批量通过/驳回 + 理由模板）、举报工单、主体认证审核、活动强制下架、敏感词库、数据看板（新增活动、有效报名、举报处置时效）。

---

## 4. 信息架构与关键页面

| 页面 | 路径 | 优先级 |
| --- | --- | --- |
| 发现首页（筛选 + 列表） | `/` | P0 |
| 活动详情（含决策卡 + 附件预览） | `/activities/:id` | P0 |
| 附件在线预览 | `/activities/:id/files/:aid` | P0 |
| 发布 / 编辑活动 | `/publish` | P0 |
| 我的（发布 / 报名 / 收藏） | `/me` | P0 |
| 登录 / 新生引导 | `/onboarding` | P0 |
| 组织者主页 | `/orgs/:id` | P1 |
| 管理后台 | `/admin` | P1 |
| 组队 / 找搭子 | `/activities/:id/teams` | P2 |

**活动详情页线框**

```
┌──────────────────────────────────────────────┐
│ ← 返回     [官方认证 ✓ 校学生会]      ⭐ 收藏  │
├──────────────────────────────────────────────┤
│ [封面图]                                      │
│ 秋季招新开放日 · 人工智能协会                  │
├──────────────────────────────────────────────┤
│ ⚡ 一页看懂                                    │
│ 收获  第二课堂学分 0.5 · 志愿工时 4h           │
│ 时长  2 小时（周三 14:00–16:00）              │
│ 门槛  零基础可参加 · 大一新生优先              │
│ 名额  已报名 32/40                            │
│ 冲突  ⚠ 与你的《高等数学》时间重叠             │
│ 往届  4.3/5（128 人评价）                      │
├──────────────────────────────────────────────┤
│ [立即报名]  [找搭子 · 已 3 人组队]  [加日历]   │
├──────────────────────────────────────────────┤
│ 📎 活动资料（3）                               │
│  招新宣讲.pptx   24 页   [在线预览] [下载]     │
│  报名须知.docx    2 页   [在线预览] [下载]     │
│  场地示意.png            [查看大图]            │
├──────────────────────────────────────────────┤
│ 活动介绍 …                                     │
│ 地点  主校区 · 三教 305   [地图导航 · 步行8分钟]│
│ 主办  人工智能协会（往届 12 场 · 均分 4.4）     │
└──────────────────────────────────────────────┘
```

---

## 5. 数据模型

核心实体与关键字段（省略通用 `id/created_at/updated_at`）：

| 实体 | 关键字段 |
| --- | --- |
| `User` | 昵称、院系、年级、认证状态、角色、兴趣标签[] |
| `Organization` | 名称、类型（官方/社团/个人/商家）、认证状态、信誉分、负责人 |
| `Activity` | 标题、摘要、主体、状态、开始/结束时间、报名截止、地点（校区/楼栋/房间/经纬度）、名额、已报名数、费用、门槛、面向人群、收获标签[]、报名方式、封面、浏览数 |
| `Attachment` | 活动、原文件名、mime、大小、存储key、转换状态、产物key、页数、抽取文本 |
| `Registration` | 活动、用户、状态、报名时间、签到码、来源（站内/外链/群） |
| `Team` / `TeamMember` | 活动、队长、人数上限、可见性、需批准；成员与加入时间 |
| `Review` | 活动、用户（须有报名记录）、评分、文字、是否实际到场 |
| `Report` | 目标类型/ID、理由、举报人、处置状态、处置动作、处理人 |
| `Favorite` / `Follow` | 用户↔活动；用户↔组织者 |
| `AuditLog` | 操作人、动作、目标、前后状态、时间 |

**Activity 状态机**（治理的关键）：

```
draft ──提交──> pending ──通过──> published ──到期自动──> ended
                   │                  │
                 驳回                取消/下架
                   ↓                  ↓
               rejected           cancelled / removed
```

---

## 6. 接口草案（REST）

| 方法 | 路径 | 说明 |
| --- | --- | --- |
| `POST` | `/api/auth/login` | 校园邮箱 / 学号认证登录 |
| `GET` | `/api/activities` | 列表：`?tag=&campus=&from=&to=&freshman=&open=&q=&sort=&page=` |
| `GET` | `/api/activities/:id` | 详情（含决策卡数据、附件列表、冲突检测结果） |
| `POST` | `/api/activities` | 创建（按主体类型进入直发或送审） |
| `PATCH` | `/api/activities/:id` | 编辑（仅主体拥有者或管理员） |
| `POST` | `/api/activities/:id/attachments` | `multipart` 上传，返回转换任务状态 |
| `GET` | `/api/attachments/:id/preview` | 查询转换状态与产物 URL |
| `POST` | `/api/activities/:id/registrations` | 报名 / 进候补 |
| `DELETE` | `/api/registrations/:id` | 取消报名 |
| `POST` | `/api/activities/:id/reviews` | 评价（校验报名记录） |
| `POST` | `/api/teams` ・ `POST /api/teams/:id/members` | 发起组队 / 加入 |
| `POST` | `/api/reports` | 举报 |
| `POST` | `/api/admin/activities/:id/approve` ・ `.../reject` | 审核 |
| `POST` | `/api/orgs/:id/claim` | 认领由运营搬运的活动 |

---

## 7. 技术选型建议

| 层 | 建议 | 理由 |
| --- | --- | --- |
| 前端 | Next.js + TypeScript + Tailwind | SSR 让活动详情页可分享、可被搜索引擎收录（学生常从微信打开链接） |
| 后端 | NestJS（或 MVP 阶段先用 Next.js API Routes） | 权限与审核逻辑会快速变复杂，独立后端更稳 |
| 数据库 | PostgreSQL + Prisma | 关系清晰；`pg_trgm` 可支撑 MVP 的中文模糊检索 |
| 对象存储 | S3 兼容（本地开发用 MinIO） | 原件与转换产物都要存，且需可签名访问 |
| 队列 | Redis + BullMQ | 转换任务必须异步，否则上传接口会超时 |
| 转换服务 | 独立容器 + LibreOffice 无头 + ClamAV | 隔离不可信文档，禁外网，限时限额 |
| 预览 | PDF.js / react-pdf | 免商业授权，逐页渲染对大 PPT 友好 |
| 检索 | MVP 用 PG 全文 + `pg_trgm`；量大后换 Meilisearch | 中文分词在 PG 需额外配置，先简后繁 |
| 部署 | Docker Compose（校内服务器） | 校内平台对数据出境与成本都敏感 |

---

## 8. 分期路线图

| 阶段 | 范围 | 目标 |
| --- | --- | --- |
| **P0**（最基本功能，2–3 周） | 账号认证、发布活动（结构化字段 + 附件上传）、**附件在线预览**（图片/txt/md 直渲，PPT/Word 转 PDF）、列表筛选与检索、详情页、我的发布 | 跑通"发布 → 浏览"闭环 |
| **P1**（信任与决策，2 周） | 一页看懂决策卡、往届评价、组织者主页、报名/候补/收藏、**过期自动归档**、举报与审核后台、主体分级 | 让信息可信、可判断 |
| **P2**（参与与增长） | 找搭子组队、新生引导与专区、`.ics` 课表冲突检测、签到、消息通知、个性化推荐 | 提升参与率与留存 |

---

## 9. 非功能需求

**安全**
- 上传白名单 + magic number 校验，拒绝宏格式，ClamAV 扫描，转换在无外网隔离容器内执行。
- 权限用 RBAC + 资源级所有权校验（编辑活动必须校验主体归属，防越权改他人活动）。
- 全站 HTTPS，附件走带签名的临时 URL，防直链盗用。

**性能**
- 列表接口 P95 < 300ms；详情页首屏 < 1.5s；附件预览首屏 < 1.5s（大文件先出前几页）。
- 图片多尺寸 + 懒加载，弱网可用。

**隐私**
- 学号仅后台可见；联系方式默认不公开，联系仅走站内私信。
- 组队成员信息最小化展示；提供注销与数据导出。
- 遵守个人信息保护相关要求，采集前明示用途。

**可访问性**
- 移动端优先（新生主要用手机），关键操作单手可达。
- 文字对比度符合 WCAG AA，支持系统字号放大；不用纯颜色传达状态（如"已结束"同时有文字）。

---

## 10. 指标体系

| 类别 | 指标 |
| --- | --- |
| 北极星 | **每周有效参与数**（站内报名，或外链点击后 7 天内确认参与） |
| 新生体验 | 引导完成率、首周人均浏览活动数、首周报名转化率、新生专区点击率 |
| 供给 | 每周新增活动数、有效发布主体数、学生个人发布占比 |
| 信息质量 | 过期活动归档及时率、举报率、审核通过率、平均评分 |
| 功能健康度 | 附件预览成功率、预览首屏耗时、转换失败率 |

---

## 11. 风险与应对

| 风险 | 影响 | 应对 |
| --- | --- | --- |
| 审核人力不足 | 低质信息泛滥 | 主体分级 + 自动化规则（敏感词、重复检测）+ 抽检，而非全量人工 |
| 冷启动无内容 | 新生看到空列表即流失 | 运营先手动搬运官方活动，配套"主办方认领"机制解决权属 |
| 学校已有类似平台 | 重复建设、推广受阻 | 定位差异化为"判断"与"学生自主发布"，并预留与现有平台互链 |
| 转换服务资源开销大 | 上传接口卡顿、成本高 | 异步队列 + 限流 + 大文件只转前若干页 |
| 上传资料版权与敏感内容 | 法律风险 | 上传即声明权属与授权；提供侵权投诉下架通道 |
| 用群聊/问卷习惯难改 | 发布量低 | 允许外链报名与群二维码，降低迁移成本（关键取舍） |

---

## 12. 待决策问题（需产品负责人确认）

1. **学号认证能否对接学校统一身份认证？** 若不能，退化为"校园邮箱验证"，会削弱身份可信度与治理能力。
2. **内容审核由谁承担？** 是否有学生运营团队？这直接决定审核策略是"先审后发"还是"先发后审 + 抽检"。
3. **是否限定单校区？** 多校区会显著增加地点字段、地图与筛选的复杂度。
4. **找搭子是否纳入第一版？** 它带来社交风险与审核成本，建议保持 P2。
5. **是否要落地代码？** 若需要，建议先实现 P0，并优先攻克"附件转换流水线"这一最高技术风险项。
